import type {HardwareProfile,Objective} from "../onboarding/types";import type{AiModel}from"../catalog/types";import type{SeedApiModel}from"../../data/seed-api-models";import{rankModels}from"./engine";
export type HybridRecommendation={mode:"local"|"api";name:string;score:number;reason:string;costNote:string};
export function recommendHybrid(profile:HardwareProfile,objective:Objective,models:AiModel[],apis:SeedApiModel[]):HybridRecommendation[]{const local=rankModels(profile,models,objective).slice(0,3).map((x)=>({mode:"local" as const,name:x.model.name,score:Math.min(100,Math.max(0,x.result.score-(profile.preference==="api"?15:0)+(profile.priority==="privacy"?10:0))),reason:x.result.reasons[0],costNote:"Sem custo por token após instalação; energia e hardware continuam contando."}));const remote=apis.map((a)=>{let score=68+(a.strengths.includes(objective)?12:0);if(profile.preference==="local")score-=15;if(profile.priority==="quality")score+=8;if(profile.priority==="privacy")score-=18;if(profile.priority==="cost")score-=Math.min(15,a.outputUsdPerM);return{mode:"api" as const,name:a.name,score:Math.round(score),reason:`API com ${a.contextK}K de contexto no catálogo demonstrativo.`,costNote:`Demo: US$ ${a.inputUsdPerM}/M entrada · US$ ${a.outputUsdPerM}/M saída.`}});return [...local,...remote].sort((a,b)=>b.score-a.score)}
