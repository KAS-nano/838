"use client";
import { useHardwareProfile } from "@/features/profile/use-hardware-profile";
import Link from"next/link";import{useMemo}from"react";import{recommendSystems}from"@/features/systems/recommender";
export default function SystemsPage(){const{profile:p}=useHardwareProfile();const objective=p.objectives[0]??"Assistente geral";const systems=useMemo(()=>recommendSystems(p,objective),[p,objective]);return <main><div className="page-container"><section><div className="page-heading"><div><p className="text-xs uppercase tracking-[.2em] text-accent">Sistema + hardware + objetivo</p><h1 className="page-title">Sistemas recomendados</h1><p className="mt-3 text-sm text-muted">Não é um ranking universal. A ordem é recalculada para {objective} e para o hardware informado.</p></div><Link href="/instalar" className="text-sm text-accent">Central de Instalação</Link></div><div className="mt-6 space-y-3">{systems.map((s,i)=><article key={s.id} className="panel p-5"><div className="grid gap-4 sm:grid-cols-[48px_1fr_90px]"><strong className="text-2xl text-muted">#{i+1}</strong><div><h2 className="text-xl font-semibold">{s.name}</h2><p className="mt-1 text-xs text-muted">Dificuldade: {s.difficulty}</p><ul className="mt-3 space-y-1 text-sm text-muted">{s.reasons.map(r=><li key={r}>✓ {r}</li>)}</ul>{s.caveats.map(c=><p key={c} className="mt-2 text-xs text-amber-200/80">Atenção: {c}</p>)}</div><strong className="text-xl">{s.score}/100</strong></div></article>)}</div></section></div></main>}
