"use client";import Link from"next/link";import{useMemo,useState}from"react";import{tools}from"@/data/tools";
export default function ToolsPage(){const[category,setCategory]=useState("all");const list=useMemo(()=>tools.filter(t=>category==="all"||t.category===category),[category]);return <main><div className="page-container"><section><div className="page-heading"><div><p className="text-xs uppercase tracking-[.2em] text-accent">Ecossistema</p><h1 className="page-title">Ferramentas, runtimes e APIs</h1></div><Link href="/dashboard" className="text-sm text-accent">Dashboard</Link></div><div className="mt-6 flex flex-wrap gap-2">{["all","runtime","ide","video","image","audio","api"].map(c=><button key={c} onClick={()=>setCategory(c)} className={`rounded-full border px-3 py-1.5 text-xs ${category===c?"border-accent/40 bg-accent/10 text-accent":"border-white/10 text-muted"}`}>{c}</button>)}</div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{list.map(t=><article key={t.slug} className="panel p-5"><div className="flex items-center justify-between gap-4"><span className="text-xs uppercase tracking-wider text-accent">{t.category}</span><span className="text-[10px] text-muted">{t.integration?"Integração preparada":"Catálogo"}</span></div><h2 className="mt-2 text-xl font-semibold">{t.name}</h2><p className="mt-3 text-sm leading-6 text-muted">{t.description}</p><p className="mt-4 text-xs text-muted">{t.systems.join(" · ")}</p></article>)}</div></section></div></main>}
