import fs from "node:fs";let fail=false;const schema=fs.readFileSync("prisma/schema.prisma","utf8");const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));const models=["User","HardwareProfile","AiModel","ModelVariant","Runtime","Benchmark","Tool","ApiModel","Recommendation","InstallationRecipe","CommunitySubmission"];const tests=[["postgres provider",schema.includes('provider = "postgresql"')],["all core models",models.every(m=>schema.includes(`model ${m} `))],["prisma pinned",pkg.devDependencies.prisma==="7.10.0"],["client pinned",pkg.dependencies["@prisma/client"]==="7.10.0"],["repository fallback",fs.readFileSync("src/server/repositories/catalog-repository.ts","utf8").includes("SeedCatalogRepository")],["database env",fs.readFileSync(".env.example","utf8").includes("DATABASE_URL")]];for(const [n,p] of tests){console.log(`${p?"PASS":"FAIL"} ${n}`);if(!p)fail=true}if(fail)throw new Error("stage7 failed");
