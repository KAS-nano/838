import fs from"node:fs";const schema=fs.readFileSync("prisma/schema.prisma","utf8"),pkg=JSON.parse(fs.readFileSync("package.json","utf8")),route=fs.readFileSync("src/app/api/auth/[...all]/route.ts","utf8");const tests=[["Prisma 7 generator",schema.includes('provider = "prisma-client"')&&schema.includes('output   = "../src/generated/prisma"')],["auth models",["Session","Account","Verification"].every(x=>schema.includes(`model ${x}`))],["Better Auth pinned",pkg.dependencies["better-auth"]==="1.7.2"&&pkg.dependencies["@better-auth/prisma-adapter"]==="1.7.2"],["auth disabled by default",fs.readFileSync(".env.example","utf8").includes('AUTH_ENABLED="false"')],["route guard before dynamic import",route.indexOf('AUTH_ENABLED')<route.indexOf('import("@/lib/auth")')],["profile export/import",fs.existsSync("src/features/profile/local-store.ts")&&fs.existsSync("src/app/perfil/page.tsx")]];let f=false;for(const[n,p]of tests){console.log(`${p?"PASS":"FAIL"} ${n}`);if(!p)f=true}if(f)throw new Error("stage18 failed");
